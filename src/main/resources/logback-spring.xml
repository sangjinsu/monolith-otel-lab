<?xml version="1.0" encoding="UTF-8"?>
<configuration>

    <springProperty scope="context" name="appName" source="spring.application.name" defaultValue="monolith-otel-lab"/>

    <!-- JSON console appender. Micrometer Tracing puts traceId/spanId into the MDC
         whenever an observation scope (e.g. an HTTP request) is active; the <pattern>
         provider below renames them to trace_id/span_id — the log field contract from
         AGENTS.md. Outside a request (startup logs etc.) both fields are empty.
         This small mapping is what makes log->trace correlation work (deep-dive §8). -->
    <appender name="JSON" class="ch.qos.logback.core.ConsoleAppender">
        <encoder class="net.logstash.logback.encoder.LoggingEventCompositeJsonEncoder">
            <providers>
                <timestamp>
                    <fieldName>timestamp</fieldName>
                    <timeZone>UTC</timeZone>
                </timestamp>
                <logLevel>
                    <fieldName>level</fieldName>
                </logLevel>
                <loggerName>
                    <fieldName>logger</fieldName>
                    <shortenedLoggerNameLength>36</shortenedLoggerNameLength>
                </loggerName>
                <message/>
                <pattern>
                    <pattern>
                        {
                          "service": "${appName}",
                          "trace_id": "%mdc{traceId}",
                          "span_id": "%mdc{spanId}"
                        }
                    </pattern>
                </pattern>
                <arguments/>
                <stackTrace/>
            </providers>
        </encoder>
    </appender>

    <root level="INFO">
        <appender-ref ref="JSON"/>
    </root>

</configuration>
